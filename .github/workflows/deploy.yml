name: Deploy to Cloudflare Workers

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest
    name: Deploy to Cloudflare Workers
    env:
      CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup pnpm
        uses: pnpm/action-setup@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: 'pnpm'

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Run linter
        run: pnpm run lint

      - name: Run formatter check
        run: pnpm run format:check

      - name: Run tests
        run: pnpm test

      - name: Build project
        run: pnpm run build

      - name: Apply D1 Migrations
        run: |
          npx wrangler d1 migrations list codiro-db --remote
          npx wrangler d1 migrations apply codiro-db --remote

      - name: Deploy to Cloudflare Workers
        run: pnpm run deploy
